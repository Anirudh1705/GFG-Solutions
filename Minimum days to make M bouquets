class Solution {
    public static int minDaysBloom(int m, int k, int[] arr) {
        // code here
        int n = arr.length;
        if(n < (m*k)){
            return -1;
        }
        Arrays.sort(arr);
        return arr[(m*k) - 1];
    }
}
